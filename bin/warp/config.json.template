{
  "verbose": ${VERBOSE},
  "bind": "${BIND}",
  "endpoint": "${ENDPOINT}",
  "key": "${KEY}",
  "dns": "${DNS}",
  "gool": ${GOOL},
  "cfon": ${CFON},
  "country": "${COUNTRY}",
  "scan": ${SCAN},
  "rtt": "${RTT}",
  "cache-dir": "${CACHE_DIR}",
  "tun-experimental": ${TUN_EXPERIMENTAL},
  "fwmark": "${FWMARK}",
  "wgconf": "${WGCONF}",
  "reserved": "${RESERVED}",
  "4": ${IPV4},
  "6": ${IPV6}
}