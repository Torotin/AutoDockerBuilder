{
  "verbose": ${VERBOSE:-false},
  "bind": "${BIND:-127.0.0.1:1080}",
  "endpoint": "${ENDPOINT:-}",
  "key": "${KEY:-}",
  "dns": "${DNS:-1.1.1.1}",
  "gool": ${GOOL:-false},
  "cfon": ${CFON:-false},
  "country": "${COUNTRY:-AT}",
  "scan": ${SCAN:-true},
  "rtt": "${RTT:-1s}",
  "cache-dir": "${CACHE_DIR:-/etc/warp/cache/}",
  "tun-experimental": ${TUN_EXPERIMENTAL:-false},
  "fwmark": "${FWMARK:-0x1375}",
  "wgconf": "${WGCONF:-}",
  "reserved": "${RESERVED:-}",
  "4": ${IPV4:-true},
  "6": ${IPV6:-false}
}